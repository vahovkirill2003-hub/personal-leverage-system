# Personal Leverage System — pilot implementation

Реализация PLS по принятому пакету спецификаций. Ты — исполнитель задач из `docs/25-implementation-task-breakdown-v0.md`. Заказчик и единственный акцептор — Кирилл.

## Нормативная иерархия (при конфликте выше побеждает ниже)

1. `docs/10-decisions-log-v2.md` — 66 продуктовых решений, неизменны
2. `docs/01…05-*-v2.md` — продуктовый baseline
3. `docs/11-technical-architecture-v1.1.md` — архитектура
4. `docs/14…22` — техспецификации (для 14 действует v0.1, для 17 — v0.1)
5. `docs/25` — task breakdown

Конфликт между слоями или невозможность реализовать как написано = ДЕФЕКТ СПЕЦИФИКАЦИИ. Остановись, опиши конфликт с точными цитатами и §, не импровизируй обход. Кирилл вернёт решение через журнал проекта.

## Жёсткие запреты

- НИКОГДА не редактируй файлы в `docs/` — это принятый канон с хешами.
- Не добавляй Redis, Kafka, Kubernetes, Temporal, микросервисы, provider-hosted tools — сознательно исключены (arch §1.3, §12.6.8).
- Не создавай команд вне реестра `docs/17…v0.1.md` §1.2 — незарегистрированный путь записи не существует.
- Модельный вывод — только proposal: ни один LLM-результат не пишет состояние без валидации `docs/18` §6.
- Никаких реальных персональных данных в фикстурах и тестах — только синтетика (`docs/22` §0.3).
- Секреты не хардкодятся, не логируются, не попадают в фикстуры.

## Порядок работы

- Строго по задачам TB-01…TB-39 из `docs/25`, по одной, в порядке зависимостей. Не начинай следующую без явного «дальше» от Кирилла.
- DoD задачи = перечисленные в её строке T-классы (`docs/22` §1) зелёные в CI + краткий отчёт.
- Тесты пишутся вместе с кодом задачи, не после фазы. Инварианты `docs/14` §5 закрываются правилами БД и тестом T04 «злонамеренный worker» (попытки обхода под ролью `pls_worker` должны отклоняться самой БД).
- Гейты фаз `docs/22` §4 не сжимаются: реальные модели не подключаются к гейтам до зелёных T01–T09, T11, T20 на mock.
- Коммиты: императив, с TB-ID (`TB-05: transactional transition protocol`). Одна задача — одна ветка/PR либо серия атомарных коммитов.

## Стек и структура

Python 3.12+, FastAPI, aiogram, PostgreSQL (единственный store и очередь: jobs + transactional outbox), pytest + hypothesis для property-тестов, Docker/OCI один image для процессов `web`/`worker`/`scheduler`, Sentry позже (TB-30). Модульный монолит: границы модулей — по компонентам arch §3.1.

## Что читать перед классами задач

- Данные/миграции (TB-04): `docs/14…v0.1.md` целиком
- Команды/переходы/гейты (TB-05…11): `docs/17…v0.1.md`, таблицы `docs/03`
- Агентный контур (TB-18…25): `docs/18`, arch §5, `docs/19` §2
- Telegram (TB-14…17): `docs/19` §1, arch §8
- Storage/backup/restore (TB-26…33): `docs/19` §4, `docs/21` §§2–4, `docs/14` §10.3

## Правило накопления

Каждый раз, когда Кирилл исправляет твою ошибку, добавляй короткое проверяемое правило в раздел ниже, чтобы она не повторилась.

## Накопленные правила проекта

(пусто — заполняется по ходу)
