# Personal Leverage System
## Журнал решений стадии реализации v0.1

**Статус:** принят пользовательской приёмкой Кирилла 21 августа 2026; действующая версия. Заменяет `26-implementation-stage-decision-log-v0.md`, который сохраняется и не редактируется  
**Дата открытия:** 13 августа 2026; **дата версии:** 14 августа 2026  
**Основание:** решение Кирилла о ведении решений стадии реализации отдельным журналом; продуктовый журнал `10-decisions-log-v2.md` остаётся принятым и неизменным  
**Область:** устойчивые решения, принятые в ходе реализации: уточнения технических спецификаций (`14`–`22`), task breakdown (`25`), распределения тестовых классов и порядка верификации  
**За рамками:** продуктовые решения, продуктовый baseline, изменение решений `PLS-001`–`PLS-066`

---

## 0. Владение и границы

Нормативная иерархия проекта, при конфликте более высокий уровень имеет приоритет:

| Уровень | Документ | Предмет |
|---|---|---|
| 1 | `docs/10-decisions-log-v2.md` | продуктовые решения `PLS-001`–`PLS-066` |
| 2 | `docs/01`–`docs/05` | продуктовый baseline |
| 3 | `docs/11-technical-architecture-v1.1.md` | архитектура |
| 4 | **настоящий журнал** | решения стадии реализации: `PLS-067` и `PLS-069`–`PLS-072` |
| 5 | `docs/14`–`docs/22` | технические спецификации |
| 6 | `docs/25` | task breakdown |

1. `docs/10` владеет продуктовыми решениями `PLS-001`–`PLS-066`. Настоящий журнал их не изменяет, не заменяет и не переиздаёт.
2. Настоящий журнал владеет решениями стадии реализации `PLS-067` и `PLS-069`–`PLS-072`. Номер `PLS-068` занят **продуктовым** решением в `10-decisions-log-v2.1.md` и настоящим журналом не используется: сквозная нумерация означает, что номер, занятый в одном журнале, недоступен в другом, а не что номера идут подряд внутри одного журнала.
3. Решение настоящего журнала может уточнять технические спецификации (уровень 5) и task breakdown (уровень 6). Оно **не может** изменять `docs/10`, продуктовый baseline (`01`–`05`) или архитектуру (`11`). Попытка такого изменения — дефект решения, а не новое правило.
4. Нумерация `PLS-*` сквозная и не дублируется между журналами: номер, занятый в одном журнале, не может быть занят в другом.
5. При конфликте настоящего журнала с уровнями 1–3 побеждают они, а решение настоящего журнала подлежит отзыву или переформулированию.

## 1. Правила журнала

- Одна запись фиксирует одно устойчивое решение и его последствия.
- Принятое решение действует, пока не заменено более новой записью настоящего журнала со ссылкой на заменяемую.
- Запись, требующая изменения принятого документа, называет документ, версию-преемника и затронутые разделы. Принятые файлы не редактируются: изменение оформляется новой версией файла рядом.
- Единственный акцептор — Кирилл.

## 2. Реестр решений

Статус `Кандидат` означает: решение сформулировано и предъявлено, но вступает в силу только с пользовательской приёмкой настоящей версии журнала. `PLS-067` показан со статусом, зафиксированным версией v0.

| ID | Решение | Статус | Последствия |
|---|---|---|---|
| PLS-067 | Распределение тестового класса T05 по задачам реализации | Принято | Уточняет применение `22` §1 и §4 и task breakdown `25`: T05 состоит из четырёх частей, каждая принадлежит своей задаче; DoD задачи включает только её часть; полный `PASS` T05 наступает только при одновременном зелёном результате всех четырёх частей на одном релизе; фазовый гейт `22` §4 не ослабляется и текст `22` не меняется |
| PLS-069 | Исполнимый контур state machine: RuleCatalog, TriggerRegistry, реестр команд, fail-closed API, `SM-APP-01` | Принято (21 августа 2026) | Уточняет `17`: каталог — исполнимая форма таблиц `03` без новых правил; пять outcome kinds; multi-target правила маршрутизируются детерминированной policy по авторитетному контексту; `SM-APP-01` вне I1–I10 и вне T02, его статический тест — архитектурный guardrail, а не физическая гарантия |
| PLS-070 | Состав и владение тестовым классом T20 (закрытие `OPEN-01`) | Принято (21 августа 2026) | Единственный релевантный для T20 `BLOCKED_*` — `BLOCKED_COVERAGE_OR_BUDGET`, статус `gate_attempt`, владелец TB-08; `PROVIDER_DATA_POLICY_BLOCKED` относится к T12/TB-20 и в T20 не входит; полный T20 на mock-контуре — TB-13 |
| PLS-071 | Распределение тестового класса T02 по задачам реализации | Принято (21 августа 2026) | I8 на границе command/state-machine — TB-06 и TB-07; отдельный regression-тест на границе model-proposal — TB-23, полный T02 он не блокирует; полный mock-PASS T02 — TB-13; состав I1–I10 и текст `22` §1 не изменяются |
| PLS-072 | Распределение тестового класса T01 и содержание его полного PASS | Принято (21 августа 2026) | Полный PASS T01 в TB-13 — full mock-contour PASS всех 78 правил на синтетических авторитетных данных и mock-адаптерах, без реальных моделей; TB-07…TB-10 дают доменные команды и контракты эффектов; TB-26 и TB-29 mock-PASS T01 не блокируют, их реальные свойства проверяются T13 и T07 позднее |

---

## PLS-067 — Распределение тестового класса T05 по задачам реализации

**Связь:** уточняет применение `22` §1 (класс T05) и `22` §4 (гейты фаз) и task breakdown `25`. Продуктовых решений не изменяет; ни одну запись `10` не заменяет и не уточняет.

### Решение

Тестовый класс T05 «Конкурентные команды» (`22` §1) состоит из четырёх независимых сценариев, механизмы которых принадлежат разным задачам реализации. Владение закрепляется так:

| Часть T05 | Владелец | Механизм |
|---|---|---|
| гонка версий | **TB-05** | транзакционный протокол `17` §3: `SELECT … FOR UPDATE`, проверка `expected_case_version`, UNIQUE `(case_id, case_version_after)` |
| parallel callbacks | **TB-16** | callback-протокол одной транзакцией: `decision_intent.token_hash` + CAS `pending→consumed` (`14` §4) |
| гонки applied | **TB-19** | partial unique `applied = true` per attempt (`14` §4) |
| двойной timer claim | **TB-29** | durable timers, claim по lease, `timer.firing_idempotency_key` (`14` §4) |

Из этого следует:

1. DoD каждой задачи включает **только** закреплённую за ней часть T05.
2. Части, принадлежащие ещё не начатым задачам, **не являются дефектом** завершённой задачи и не блокируют её приёмку.
3. До закрытия всех четырёх частей класс T05 имеет статус **`PARTIAL`** с явным перечнем закрытых и незакрытых частей в отчётности.
4. Полный **`PASS`** T05 наступает только при одновременном зелёном результате всех четырёх частей **на одном релизе** и фиксируется записью настоящего журнала, а не отчётом исполнителя отдельной задачи.

Критерий PASS класса из `22` §1 — «Ровно один эффект; вторые получают conflict/прежний outcome» — не изменяется и применяется к каждой части отдельно.

### Влияние на фазовые гейты

Текст `22` §4 **не изменяется**, и гейт не ослабляется. Формулировка «Подключение реальных моделей к гейтам — T01–T09, T11, T20 PASS на mock-контуре» читается как требование **полного** PASS T05 в смысле п. 4 выше.

Операционное следствие фиксируется явно:

- перед подключением реальных моделей к гейтам должен существовать полный PASS T05;
- поэтому части, принадлежащие **TB-16**, **TB-19** и **TB-29**, должны быть выполнены до этого гейта, даже если сами задачи перечислены в более поздних тематических фазах `25` (TB-16 — фаза 2, TB-19 — фаза 3, TB-29 — фаза 5);
- размещение задачи в разделе фазы `25` не отменяет её зависимостей и не позволяет обойти более ранний фазовый гейт: тематические разделы группируют работу по предмету, а не задают исчерпывающий порядок относительно гейтов.

Владельцы частей T05 находятся **в разных фазах** — это распределение между фазами, а не внутри одной фазы. Формулировка «распределение внутри одной фазы» ошибочна и не должна использоваться при ссылках на настоящее решение.

### Обоснование

`22` §1 задаёт T05 как единый класс по природе проверяемого свойства (конкурентность), а не по слою реализации. Механизмы четырёх сценариев появляются в разные фазы `25`, поэтому единый неделимый DoD либо принуждает исполнителя к преждевременной реализации чужих задач в обход порядка зависимостей, либо оставляет завершённую задачу формально незакрытой без содержательной причины.

Распределение сохраняет полноту класса как гейта: ни один сценарий не исчезает, критерий PASS не смягчается, гейт `22` §4 не сжимается. Изменяется только адресация работы и момент объявления полного PASS.

Прецедент дробления уже присутствовал в `25` v0 (строка TB-19: «T05 (гонки applied)») без соответствующей пометки в строке TB-05, что и породило расхождение. Настоящее решение делает дробление явным правилом и устраняет умолчание.

### Затронутые документы

| Документ | Действие |
|---|---|
| `25-implementation-task-breakdown-v0.md` | заменён версией-преемником `25-implementation-task-breakdown-v0.1.md`: строки TB-05, TB-16, TB-19, TB-29; правила задач (task-local DoD распределённого класса); примечание к фазе 1 и отсылки в фазах 2, 3, 5; критический путь; traceability; changelog v0.1. Принятый файл v0 не редактируется |
| `22-verification-acceptance-test-plan-v0.md` | **не изменяется**: текст класса T05, критерий PASS и гейты §4 остаются как приняты |
| `10-decisions-log-v2.md` | **не изменяется** |

### Порядок фиксации полного PASS T05

Полный PASS объявляется новой записью настоящего журнала. Запись обязана назвать: идентификатор релиза и CI-run, в котором зелены все четыре части одновременно; перечень тестов по каждой части; подтверждение, что критерий `22` §1 выполнен для каждой части отдельно. До появления такой записи любое утверждение «T05 PASS» в отчётности задач считается ошибочным; корректная формулировка — `T05 PARTIAL` с перечнем.

---

---

## PLS-069 — Исполнимый контур state machine: RuleCatalog, TriggerRegistry, реестр команд, fail-closed API, `SM-APP-01`

**Связь:** уточняет `17` (версия-преемник `17-state-gate-authority-tech-spec-v0.2.md`, §§1.3 и 7.1). Продуктовых решений не изменяет. Нормативным первоисточником правил остаётся `03`.

### Решение

1. **RuleCatalog содержит ровно 78 правил** — исполнимую форму таблиц `03`, без добавления, удаления и переформулирования правил. Состав: §7 — 27, §8 — 21, §9 — 16, §10.1 — 6, §10.2 — 4, §10.3 — 2, §16.1 — 2. Двенадцать строк §11 «Запрещённые и опасные переходы» правилами каталога не являются и закрываются классом T20; полный разбор таблиц `03` даёт 90 строк = 78 + 12.
2. **Outcome kinds** правил каталога: `STATE_TRANSITION`, `SELF_TRANSITION`, `ADMINISTRATIVE_DISPOSITION`, `TERMINAL_ANNOTATION`, `LINKED_CASE_CREATION`. У каждого правила ровно один вид исхода.
3. **Multi-target правило** остаётся одним правилом: обе цели допустимы, ни одна не удаляется, цель вычисляется детерминированной routing policy по авторитетному контексту кейса и никогда не принимается от вызывающей стороны. Нормативное место policy — кандидат `03-state-machine-v2.1.md` §9.1.
4. **Реестр команд расширен с 24 до 40.** Правила `03`, не имевшие командного пути в `17 v0.1` (завершение подготовительных стадий, маршрутизация возвратов и revalidation, разрешение эскалации и review condition, нагрузка, паузы, внешнее ожидание, дополнительное время, блокировка неподтверждённого расхода), получают доменные команды. Покрытие 78/78 доказывается таблицей `17 v0.2` §1.5; ни одна команда v0.1 не удалена и не переформулирована.
5. **TriggerRegistry — закрытый тип входа.** 66 триггеров: 47 продуктовых событий `03` §4, используемых правилами, разделение `DECISION_DEFERRED` по стороне ожидания, `ADDITIONAL_TIME_APPROVED` и 17 технических идентификаторов прозаических правил. Любой иной вход отклоняется до чтения состояния. Пять событий каталога, не связанных ни с одним правилом, остаются журнальными и правил не запускают. `ADDITIONAL_TIME_APPROVED` регистрируется технически, потому что без него правило R67 неисполнимо; в продуктовый каталог `03` §4 он **не** добавляется — это незакрытая `AUD-N2` и предмет продуктовой errata к следующей версии `03`.
6. **Fail-closed исполнительный API.** Единственная публичная точка записи — `execute_trigger(case_id, expected_case_version, trigger_id, actor, idempotency_key, payload)`. В сигнатуре нет `to_state`, `rule_id` и перечня guards; поле вне схемы отклоняет вход целиком. После lock выполняется loader авторитетного контекста, машина разрешает ровно одно правило, guards берутся из правила, цель multi-target вычисляется предикатом, полный effect bundle применяется приватным commit. Ноль или несколько кандидатов правила — отказ без частичных эффектов. Функция применения перехода приватна: передать в неё произвольные `to_state` и guards снаружи невозможно по контракту API.
7. **Commit-протокол зависит от outcome kind.** Loader, resolver и выбор guards общие для всех правил, но применение эффектов различается: transition protocol для `STATE_TRANSITION`/`SELF_TRANSITION`; non-transition commit для семи `ADMINISTRATIVE_DISPOSITION` (без `state_transition`, без изменения `case` и версии); late-evidence commit для `TERMINAL_ANNOTATION` (case и версия не меняются, инвариант I6); linked-case commit (старый кейс не изменяется); creation protocol для R01, где строки `case` ещё нет и сериализация выполняется уникальными ограничениями slot и idempotency, а не `SELECT … FOR UPDATE`. Единый транзакционный протокол для всех исходов требовал бы `state_transition` там, где правила его запрещают.
8. **`SM-APP-01`** — технический application-инвариант: единственный путь записи состояния — команды реестра `17` §1.2 через транзакционный протокол §3. Он **не является** нормативным инвариантом: не входит в I1–I10 и не входит в T02, состав которого не изменяется. Его проверка — обязательный архитектурный guardrail (статический тест над исходным кодом), утверждающий отсутствие обходного пути записи в кодовой базе, а не физическую невозможность прямого `INSERT`. Физические гарантии дают правила и роли БД `14` §5/§12 и классы T03/T04.

### Обоснование

Реализация обязана иметь машинно-проверяемое соответствие таблицам `03` — иначе «покрытие всех переходов» недоказуемо. Каталог фиксирует счёт и разложение, а не создаёт нормативный дубликат. Разделение outcome kinds необходимо потому, что четыре из пяти видов исхода не являются сменой состояния и без явной классификации либо теряются, либо ошибочно моделируются как переходы.

Отказ от нормативного инварианта `I11` намеренный: физическую гарантию невозможности прямого `INSERT` прикладной статический тест дать не может, а запись такого утверждения в I1–I10 создала бы ложное доказательство в T02.

### Затронутые документы

| Документ | Действие |
|---|---|
| `17-state-gate-authority-tech-spec-v0.1.md` | заменён преемником `17-state-gate-authority-tech-spec-v0.2.md` (§1.2 дополнение, §§1.3–1.5, §§3.1–3.8, §7.1). Принятый файл v0.1 не редактируется |
| `14-data-model-persistence-spec-v0.2.md` | заменён преемником `14-data-model-persistence-spec-v0.3.md`: технические errata `DMV-4`–`DMV-6` (адресный `pre_experiment_dismissal.case_id`, genesis-`from_state`, `command_receipt`). Нового `PLS-ID` не требуется — исполнимость уже принятых правил, не новое решение. Владелец миграции — TB-04a `25 v0.2` |
| `03-state-machine-v2.1.md` | закрытие `AUD-N2` редакционной erratum (каталог событий 52, journal-only квалификация) — **продуктовое основание**, принимается актом `09 v0.1` §5.1; настоящий журнал его не выносит. Следствие для стадии: TriggerRegistry `17 v0.2` §1.4 равен 65 = 52 − 5 + 1 + 17 |
| `03-state-machine-v2.md` | **не изменяется**. Преемник `03-state-machine-v2.1.md` содержит только редакционное уточнение маршрутизации и принят отдельно актом `09 v0.1` 21 августа 2026 (раздел 3 настоящего журнала) |
| `22-verification-acceptance-test-plan-v0.md` | **не изменяется**: состав T02 и тексты классов остаются как приняты |

---

## PLS-070 — Состав и владение тестовым классом T20 (закрытие `OPEN-01`)

**Связь:** уточняет применение `22` §1 (класс T20) и task breakdown `25`. Текст `22` не изменяет. Применяет правило распределённого T-класса `PLS-067`.

### Вопрос `OPEN-01`

При пересборке пакета остался неразрешённым состав T20: какие состояния `BLOCKED_*` входят в класс «negative-paths» и какая задача ими владеет.

### Решение

1. Единственный релевантный для T20 `BLOCKED_*` — **`BLOCKED_COVERAGE_OR_BUDGET`**. Это статус `gate_attempt` при недостатке покрытия или бюджета (архитектура §10 и §12, `12` §5.4): `PASS` при нём невозможен. Владелец части — **TB-08** (Gate Engine).
2. **`PROVIDER_DATA_POLICY_BLOCKED`** относится к Data Policy Registry и preflight-политике провайдеров, проверяется классом **T12** в задаче **TB-20** и в состав T20 **не входит**.
3. Полный PASS T20 на mock-контуре наступает в **TB-13** и покрывает все запрещённые маршруты §11 `03`, границы `PRE_EXPERIMENT_DISMISSED`, недостижимость прямого `CLOSED_NOT_COUNTED` и действия после абсолютного deadline.
4. До закрытия обеих частей класс имеет статус `PARTIAL` с перечнем; полный `PASS` фиксируется записью настоящего журнала.

`OPEN-01` закрыт.

---

## PLS-071 — Распределение тестового класса T02 по задачам реализации

**Связь:** уточняет применение `22` §1 (класс T02) и `25`. Состав инвариантов I1–I10 и текст `22` не изменяются. Применяет правило `PLS-067`.

### Решение

| Часть T02 | Владелец | Содержание |
|---|---|---|
| I1 один `gate_record` на attempt | **TB-08** | Gate Engine |
| I2 set-once anchor | **TB-10** | `experiment_anchor` |
| I3 единственный slot | **TB-06** | доменная проверка `G01`; DB-правило — TB-04 |
| I4 terminal lock | **TB-06** | терминальные состояния не покидаются; DB-правило — TB-04 |
| I5 counter не сбрасывается | **TB-08** | `tr_return_counter` |
| I6 late evidence не меняет case | **TB-07** | `RegisterLateEvidence`, правило R77 |
| I7 linked case без наследования | **TB-07** | `CreateLinkedCase`, правило R78 |
| I8 команда не принимает model text как значение состояния | **TB-06**, **TB-07** | граница command/state-machine |
| I9 расход exact-bound и одноразовый | **TB-09** | Consent/Authority Engine |
| I10 hold не двигает время | **TB-11** | diagnostic hold |
| regression на границе model-proposal | **TB-23** | отдельный regression-тест конвейера валидации `18` §6 |
| полный mock-PASS | **TB-13** | property-прогон I1–I10 одной генерацией на mock-контуре |

Каждый из десяти инвариантов имеет task-local владельца; ни один не остаётся без адресата, поэтому формулировка «T02 распределён» соответствует таблице.

Часть TB-23 **не блокирует** полный T02: её предмет — граница модельного предложения, тогда как полнота класса определяется property-прогоном I1–I10. Отсутствие TB-23 не является дефектом реализации TB-06, TB-07 или TB-13, а её наличие не заменяет полный прогон.

`SM-APP-01` в T02 не входит (`PLS-069`).

---

## PLS-072 — Распределение тестового класса T01 и содержание его полного PASS

**Связь:** уточняет применение `22` §1 (класс T01) и `25`. Текст `22` не изменяет. Применяет правило `PLS-067`.

### Решение

1. Полный PASS T01 наступает в **TB-13** и называется **full mock-contour PASS**: все 78 правил RuleCatalog исполняются с **синтетическими авторитетными данными** и **mock-адаптерами инфраструктуры**, без реальных моделей.
2. **TB-07…TB-10** предоставляют доменные команды и контракты их эффектов, без которых правила каталога неисполнимы; их части T01 закрываются в своих задачах.
3. **TB-26** (storage) и **TB-29** (scheduler runtime) полный mock-PASS T01 **не блокируют**: на mock-контуре их адаптеры заменяются синтетическими. Реальные свойства storage и scheduler проверяются позднее собственными классами — T13 и T07 соответственно.
4. Пункт 3 не ослабляет `PLS-067`: часть T05, закреплённая за TB-29, остаётся обязательной до гейта `22` §4 «Подключение реальных моделей к гейтам».
5. Формулировка «T01 PASS» без уточнения контура в отчётности задач некорректна: до TB-13 — `PARTIAL` с перечнем, после TB-13 — `full mock-contour PASS`.

---

## 3. Продуктовое решение, вводящее `03-state-machine-v2.1.md`

Настоящий журнал (уровень 4) не может изменять продуктовый baseline (уровень 2), поэтому маршрутизация `RELEVANCE_LOST` после входа в Experiment Planning оформлена **продуктовым** решением `PLS-068` в кандидате `10-decisions-log-v2.1.md`, а не записью настоящего журнала.

| Предмет | Носитель | Статус |
|---|---|---|
| `PLS-068` — детерминированная маршрутизация `RELEVANCE_LOST` после Planning | `10-decisions-log-v2.1.md` (уровень 1) | Принято актом `09 v0.1` 21 августа 2026 |
| `03-state-machine-v2.1.md` — §9 и §9.1 как следствие `PLS-068` | продуктовый baseline (уровень 2) | Введена в baseline актом `09 v0.1` 21 августа 2026 |

Квалификация изменения: **новое продуктовое решение**, не редакционная правка. Обе цели правила сохранены, но множество случаев, в которых выбирается каждая, изменено — внутренние наблюдения без внешнего действия больше не дают отдельного захода в `EVIDENCE_COLLECTION`. Основание квалификации и её последствия изложены в `PLS-068`.

Решения `PLS-069`–`PLS-072` настоящего журнала от исхода приёмки `PLS-068` не зависят: счёт 78 правил, outcome kinds, реестр триггеров и распределение T-классов одинаковы для `03 v2` и `03 v2.1`. Ссылка на §9.1 в `17 v0.2` при неприёмке `03 v2.1` читается как ссылка на нормативное место будущей версии, а не как основание исполнять несуществующее правило.

## 4. Changelog v0.1

| Изменение против v0 | Основание |
|---|---|
| Добавлены записи `PLS-069`–`PLS-072` и соответствующие строки реестра | пересборка пакета от 14 августа 2026 |
| Добавлен раздел 3: `03-state-machine-v2.1.md` вводится продуктовым решением `PLS-068` в `10 v2.1`, а не записью настоящего журнала | граница уровня 4 (раздел 0) |
| Номер `PLS-068` отдан продуктовому журналу; записи стадии занимают `PLS-069`–`PLS-072`; правило владения номерами в разделе 0 уточнено | сквозная нумерация `PLS-*` |
| Пояснён статус `Кандидат` в реестре решений | статус документа |
| `PLS-069` дополнен пунктом 7: commit-протокол зависит от outcome kind, включая creation protocol для R01 | архитектурный разрыв единого протокола |
| В затронутые документы `PLS-069` добавлена `14 v0.3` с errata `DMV-4`–`DMV-6` | сверка `17 v0.2` §§3.4, 3.7 с фактической схемой |
| Права на `command_receipt` закреплены за `pls_worker` (SELECT + INSERT), у `pls_web` их нет; исполнитель команд — worker | архитектура §1.1: web ставит задание, не исполняет команду |
| TB-04a зависит от TB-04; принятая строка TB-05 сохраняет зависимость `TB-04`, совместимость с `14 v0.3` подтверждается DoD TB-04a; от TB-04a зависят TB-06 и TB-07 | запрет ретроактивной зависимости уже выполненной задачи |

Текст `PLS-067` не изменён. Продуктовые решения `PLS-001`–`PLS-066` не изменены. Тексты `22` §1 и `22` §4 не изменены.

---

*Журнал не изменяет продуктовый baseline и решения `PLS-001`–`PLS-066`; расхождения с ними возвращаются Кириллу как дефект решения настоящего журнала.*
