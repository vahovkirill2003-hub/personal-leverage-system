# Personal Leverage System
## Implementation Task Breakdown v0.2

**Статус:** принят пользовательской приёмкой Кирилла 21 августа 2026; действующая версия. Заменяет `25-implementation-task-breakdown-v0.1.md` в части строк TB-06…TB-11, TB-13, TB-23, новой задачи TB-04a, примечания к фазе 1 и traceability. В остальном идентична v0.1; v0 и v0.1 сохраняются и не редактируются. TB-04a и TB-06 не начинать до отдельного решения Кирилла  
**Дата:** 14 августа 2026  
**Основание:** пакет спецификаций `14 v0.3`, `17 v0.2`, `18`–`22` (v0), архитектура v1.1, вердикт `PASS` аудита `24`, разрешение записи 10 журнала `16`, решения `PLS-067`–`PLS-072` журнала стадии реализации `26 v0.1`  
**Область:** упорядоченные задачи реализации с зависимостями, источниками и верификацией; гейты фаз по `22` §4  
**За рамками:** распределение инструментов разработки (отложено `PLS-017`/журналом — при желании отдельный Development Process doc), код, конфигурация провайдеров до проверок §20.2

**Правила задач:** одна задача — один результат с DoD; DoD всегда включает зелёные указанные T-классы (`22` §1) в CI; задача не начинается до завершения зависимостей; изменение спецификаций из задачи запрещено — расхождение возвращается как дефект спецификации через журнал. Размер: S ≤ 0.5 дня, M ≤ 2 дней, L ≤ 5 дней одного исполнителя.

**Task-local DoD распределённого T-класса (`PLS-067`).** Если T-класс распределён по задачам решением журнала стадии, DoD задачи включает только закреплённую за ней часть класса; части, принадлежащие другим задачам, не входят в её DoD и не являются дефектом её реализации. До закрытия всех частей класс имеет статус `PARTIAL` с явным перечнем закрытых и незакрытых частей. Полный `PASS` распределённого класса — отдельный факт: он наступает только при одновременном зелёном результате всех частей на одном релизе и фиксируется записью журнала стадии, а не отчётом исполнителя отдельной задачи. Критерии PASS `22` §1 и тексты фазовых гейтов `22` §4 этим правилом не изменяются.

## Фаза 0 — каркас (гейт входа: принятие настоящего документа)

| ID | Задача | Зависит | Источники | Проверка | Размер |
|---|---|---|---|---|---|
| TB-01 | Репозиторий, структура модульного монолита, lockfile c хэшами, CI (lint, tests, migration dry-run, build одного OCI image) | — | §14.1, `20` §2.4, `21` §1 | CI зелёный на пустом ядре | M |
| TB-02 | Локальная dev-среда: контейнеры app+PostgreSQL, синтетические fixtures-заготовки | TB-01 | §15.1, `22` §0.3 | Поднимается одной командой | S |
| TB-03 | Каркас трёх процессов (web/worker/scheduler) из одного image; health/liveness/readiness | TB-01 | §1.1, `19` §6, §15.4 | Probes по спецификации | M |

## Фаза 1 — детерминированное ядро (гейт: T01–T09, T11, T20 PASS на mock)

**Примечание к T05 (`PLS-067`) — распределение между фазами.** Класс T05 распределён по четырём задачам, находящимся в **разных** фазах настоящего документа: TB-05 «гонка версий» (фаза 1), TB-16 «parallel callbacks» (фаза 2), TB-19 «гонки applied» (фаза 3), TB-29 «двойной timer claim» (фаза 5). Гейт `22` §4 «Подключение реальных моделей к гейтам» требует `T01–T09, T11, T20 PASS`, то есть **полного** PASS T05 в смысле `PLS-067`. Следовательно части TB-16, TB-19 и TB-29 должны быть выполнены до этого гейта, несмотря на размещение самих задач в более поздних тематических разделах. Размещение задачи в разделе фазы не отменяет её зависимостей и не позволяет обойти более ранний фазовый гейт; тематические разделы группируют работу по предмету, а не задают исчерпывающий порядок относительно гейтов.

**Примечание к T01, T02 и T20 (`PLS-070`–`PLS-072`).** Три класса распределены по задачам фазы 1 по тому же правилу `PLS-067`. Полный PASS каждого из них наступает в **TB-13** и означает PASS на mock-контуре: все 78 правил RuleCatalog исполняются с синтетическими авторитетными данными и mock-адаптерами инфраструктуры, без реальных моделей. TB-07…TB-10 предоставляют доменные команды и контракты эффектов, без которых правила неисполнимы. TB-26 (storage) и TB-29 (scheduler runtime) полный mock-PASS T01 **не блокируют**: их адаптеры на mock-контуре синтетические, а реальные свойства storage и scheduler проверяются позднее собственными классами T13 и T07. Часть T05, закреплённая за TB-29 решением `PLS-067`, остаётся обязательной до гейта подключения реальных моделей и этим примечанием не ослабляется. `PROVIDER_DATA_POLICY_BLOCKED` в T20 не входит: он относится к T12 и проверяется в TB-20.


| ID | Задача | Зависит | Источники | Проверка | Размер |
|---|---|---|---|---|---|
| TB-04 | Миграции модели данных: все сущности `14` §3 + database rules §5 (append-only, set-once, slot, terminal, immutable-колонки) и DB-роли §12 | TB-02 | `14` | T03, T04 | L |
| TB-04a | Миграция схемы под `14 v0.3` (expand/contract, раздел 8): `pre_experiment_dismissal.case_id` NOT NULL + FK + UNIQUE; `state_transition.from_state` DROP NOT NULL + genesis-CHECK; таблица `command_receipt` с UNIQUE(command_type, idempotency_key), append-only trigger и привилегиями (`pls_worker` SELECT + INSERT; `pls_web` прав не имеет) | TB-04 | `14 v0.3` §§3.1, 3.3, 4, 5, 8, 12 | T03, T04 (в T04 — разрешённые SELECT/INSERT в `command_receipt` под `pls_worker` и отклонение UPDATE/DELETE самой БД); **повторный прогон всех тестов TB-05 на новой схеме** | S |
| TB-05 | Event/State Transition ledger + транзакционный протокол `17` §3 (lock, version, guards-запись, атомарность) | TB-04 | `17 v0.2` §§2, 3.1–3.8; `14 v0.3` §3.3 | T01 частично, T05 частично (гонка версий) — `PLS-067` | M |
| TB-06 | State Machine: RuleCatalog из 78 правил `03` (`17 v0.2` §1.3), закрытый TriggerRegistry §1.4, резолвер правила и guards, routing policy multi-target, исполнительный API `17 v0.2` §§3.1–3.8 (публичный `execute_trigger`, приватный commit effect bundle, отдельные commit-протоколы по outcome kind, включая creation protocol R01, non-transition, late-evidence и linked-case) | TB-05, TB-04a | `03`, `17 v0.2` §§1.3–1.5, 2, 3 | T01 частично (правила и guards), T02 частично (I3, I4, I8), T20 частично (запрещённые маршруты §11 `03`) — `PLS-070`–`PLS-072` | L |
| TB-07 | Реестр команд `17 v0.2` §1.2 (все 40: 24 из v0.1 + 16 доменных команд правил, включая admin) + покрытие 78/78 по §1.5 + idempotency-реестр `14` §4 (`command_receipt`) | TB-05, TB-04a | `17 v0.2` §§1.2, 1.5 | T06, T01 частично (доменные команды и контракты эффектов), T02 частично (I6, I7, I8) — `PLS-071`, `PLS-072` | M |
| TB-08 | Gate Engine: attempts, sessions, coverage cells, findings, Coverage Validator, один record, counter-view, эскалация, статус attempt `BLOCKED_COVERAGE_OR_BUDGET` | TB-06 | §5.4, `14` §3.4, `17 v0.2` §4 | T10 (детерминированная часть), T11, T20 частично (`BLOCKED_COVERAGE_OR_BUDGET`) — `PLS-070`, T02 частично (I1, I5) — `PLS-071` | L |
| TB-09 | Consent/Authority Engine: intents (issue/CAS-consume), consents, grants, expenses, workload ledger (ISO-неделя) | TB-06 | `17 v0.2` §4, `14` §3.5, `16` зап.2 | T09 (доменная часть), T06, T01 частично (контракты эффектов полномочий) — `PLS-072`, T02 частично (I9) — `PLS-071` | L |
| TB-10 | Time: preparation intervals/union, time grants, 45/60-события, anchor/day-14 guards, timer-таблица | TB-06 | §10, `14` §3.7, `17 v0.2` §6 | T07, T08, T01 частично (контракты временных эффектов) — `PLS-072`, T02 частично (I2) — `PLS-071` | L |
| TB-11 | Diagnostic hold + invariant-monitor под read-only ролью + compensating-команды | TB-05 | `17 v0.2` §5, `14` §10.2, `20` §2.2, `21` §4 | T18, T02 частично (I10) — `PLS-071` | M |
| TB-12 | Mock-агенты всех ролей (детерминированные заглушки конвертов `18`) для прогона ядра без моделей | TB-07 | `18` §§1–2 | T19 на mock | M |
| TB-13 | Полный e2e mock-цикл Intake→CLOSED_COUNTED/NOT_COUNTED, возвраты, пауза, resumption, late evidence, linked case, pre-experiment dismissal; прогон всех 78 правил RuleCatalog на синтетических авторитетных данных и mock-адаптерах инфраструктуры | TB-06..12 | §7, `03`, `17 v0.2` §§1.3, 10.1 | T19, **T01 полный (full mock-contour PASS)**, **T02 полный mock-PASS**, **T20 полный на mock** — `PLS-070`–`PLS-072` | L |

**Примечание к TB-04a и уже выполненной TB-05.** TB-04a добавлена после того, как TB-05 был реализован и принят на схеме `14 v0.2`, поэтому его строка сохраняет принятую зависимость `TB-04`: ретроактивная зависимость создавала бы ложное впечатление, что TB-05 принимался с нарушенным порядком. Совместимость ledger-а и транзакционного протокола с `14 v0.3` подтверждается не переоформлением TB-05, а DoD самой TB-04a — повторным прогоном всех тестов TB-05 на мигрированной схеме. От TB-04a зависят задачи, которые впервые используют новые объекты схемы: TB-06 (creation protocol R01 с `from_state IS NULL` и административное прекращение по `pre_experiment_dismissal.case_id`) и TB-07 (idempotency-реестр на `command_receipt`).

## Фаза 2 — Telegram (гейт: T09 транспортная часть PASS на test-боте)

*Часть T05 «parallel callbacks» в TB-16 обязательна до гейта подключения реальных моделей — см. примечание к фазе 1 (`PLS-067`).*

| ID | Задача | Зависит | Источники | Проверка | Размер |
|---|---|---|---|---|---|
| TB-14 | Webhook: secret, allowlist, inbox-дедупликация, поведение при недоступной БД | TB-03, TB-07 | `19` §1.1 | T06, T09 | M |
| TB-15 | Outbox-рендеринг informational/decision сообщений, повторная доставка, замена сообщений | TB-09 | `19` §1.2, §8.2/8.6 | T14-смежные фикстуры вёрстки, T06 | M |
| TB-16 | Callback-протокол одной транзакцией + все no-op исходы §8.4 | TB-09, TB-14 | `19` §1.3 | T09 полный, T05 частично (parallel callbacks) — `PLS-067` | M |
| TB-17 | Transport maintenance mode и ротация секретов; SupersedePendingIntents | TB-15 | `19` §1.4, `17 v0.1` | Ручной drill по `21` §7 на test | S |

## Фаза 3 — агентный контур (гейт: T12 выбранных adapters + базовые T14/T15)

*Часть T05 «гонки applied» в TB-19 обязательна до гейта подключения реальных моделей — см. примечание к фазе 1 (`PLS-067`).*

| ID | Задача | Зависит | Источники | Проверка | Размер |
|---|---|---|---|---|---|
| TB-18 | Context Builder: allowlists, redaction+report, бюджеты, manifest, package_hash | TB-04 | `18` §3, §12.2 | T15 | M |
| TB-19 | Model Gateway ядро: внутренний контракт, Cost Controller preflight, call-лимиты §13.2, dual-response правило | TB-07 | `19` §2, §13.2 | T05 частично (гонки applied) — `PLS-067`, T17 частично | M |
| TB-20 | Data Policy Registry: схема + первичное датированное заполнение по актуальным официальным условиям OpenAI/Anthropic (retention, training, location, deletion, `store:false`) + fail-closed evaluator | TB-19 | §12.6.4–6, §14.3 | T12 policy-часть | M |
| TB-21 | Native adapters OpenAI и Anthropic + общие conformance-фикстуры + failover | TB-19, TB-20 | `19` §2.2, §14.3 | T12 | L |
| TB-22 | Tool & Fact Gateway: 4 tool-а, allowlists, snapshots, denials | TB-18 | `18` §4, `19` §3 | T14 tool-часть | M |
| TB-23 | Agent Runtime: runs, purposes, ancestry, делегирование depth-1, конвейер валидации `18` §6, format repair | TB-19, TB-12 | `18` §§5–6, §5.1 | T11, T06, отдельный regression-тест T02 на границе model-proposal (полный T02 не блокирует) — `PLS-071` | L |
| TB-24 | Fixture-наборы evals: засеянные дефекты TR (≥20), пограничные gaps SA, injection, minimization, oversized-досье с cross-area противоречиями | TB-12 | `22` §2, `18` §7 | Сами являются T10/T14/T15 | L |
| TB-25 | Прогон evals §2 `22` на кандидатных конфигурациях модель+промпт; фиксация проходных конфигураций | TB-21..24 | `22` §2 | Пороги §2 | M |

## Фаза 4 — evidence и storage (гейт: T13 в целевой конфигурации)

| ID | Задача | Зависит | Источники | Проверка | Размер |
|---|---|---|---|---|---|
| TB-26 | Evidence/Artifact Service: content-addressed keys, SHA-256 до/после, quarantine, файловые правила §12.4 | TB-04 | §9.4, §12.4, `14` §3.6 | T13 application-часть | M |
| TB-27 | Storage adapter (кандидат R2) + полный conformance-suite §9.4, включая негативные | TB-26 | `19` §4 | T13 | M |
| TB-28 | Retention/deletion процедура + tombstones + retention-роль | TB-26 | `20` §§3,5, `14` §9 | Тест процедуры | S |

## Фаза 5 — эксплуатация (гейт: первый T16 PASS)

*Часть T05 «двойной timer claim» в TB-29 обязательна до гейта подключения реальных моделей — см. примечание к фазе 1 (`PLS-067`).*

| ID | Задача | Зависит | Источники | Проверка | Размер |
|---|---|---|---|---|---|
| TB-29 | Scheduler: durable timers, claims, overdue-scan, leader lease, business-day календарь | TB-10 | §10.4, `14` §3.7 | T07, T05 частично (двойной timer claim) — `PLS-067` | M |
| TB-30 | Observability: structured logs c redaction, метрики §13.1, traces, алёрты P1–P3 | TB-03 | §13.1, `21` §6 | Проверка redaction-фильтра | M |
| TB-31 | Cost Records/каталог цен/envelope-схема + пороги и деградация | TB-19 | §13.2–13.3, `14` §3.8 | T17 | M |
| TB-32 | Backup-контур: dumps, шифрование, ключи по `20` §4, export-manifest | TB-26 | §15.3, `21` §2 | Часть T16 | M |
| TB-33 | Restore-процедура `21` §3 как исполняемый runbook-скрипт (sibling, integrity, post-restore реконсиляция, divergence report) + первый drill | TB-32, TB-11 | `14` §10.3, `21` §3 | T16 | L |
| TB-34 | Diagnostic view / admin CLI read-only | TB-05 | §13.4 | Ручная проверка | S |

## Фаза 6 — production readiness (гейт: чек-лист `21` §8 целиком + §20.2 + независимый аудит по записи 9)

| ID | Задача | Зависит | Источники | Проверка | Размер |
|---|---|---|---|---|---|
| TB-35 | Проверки §20.2: оплата/доступность/data policy провайдеров deployment/DB/storage/models из фактического места эксплуатации | TB-20, TB-27 | §20.2, §14.2 | Датированные записи | M |
| TB-36 | Датированный cost envelope §13.3 + отдельное решение Кирилла об активации | TB-31, TB-35 | §13.3, `21` §8 | Чек-лист без пропусков | S |
| TB-37 | Независимый сводный аудит (перенесённый записью 9): пакет спецификаций + код + отчёт тестов, отдельный чат, File Integrity Gate | все | `16` зап.9, `24` §5.3 | Вердикт PASS/PASS WITH RC | M |
| TB-38 | Production deploy, restore rehearsal на production-конфигурации, приёмка Кириллом готовности к первому кейсу | TB-33, TB-35..37 | `22` §4, §15 | Все T зелёные | M |
| TB-39 | Сопровождение errata: `12` (PAV-1) и `14 v0.2` (PAV-2) при первом же содержательном изменении этих документов | — | `24` §2 | Запись журнала | S |

## Критический путь

TB-01→04→(05 ∥ 04a)→06→(07,08,09,10)→13 → 14–16 → 18–25 → 33 → 35–38. Оценка сумм: ядро (ф.0–1) ≈ 15–20 дней одного исполнителя; полный путь до production readiness ≈ 35–45 дней календарно при агентной разработке существенно быстрее — но гейты фаз не сжимаются.

**Гейт подключения реальных моделей и распределённый T05 (`PLS-067`).** Гейт `22` §4 «Подключение реальных моделей к гейтам» требует полного PASS T05, части которого принадлежат TB-05, TB-16, TB-19 и TB-29. Поэтому на критическом пути к этому гейту лежат, помимо ядра, ещё три задачи из более поздних тематических разделов: **TB-16** (фаза 2), **TB-19** (фаза 3) и **TB-29** (фаза 5, зависит от TB-10). Ни одна из них не может быть отложена «за гейт» на основании номера своей фазы. Порядок внутри самого гейта свободен: части T05 независимы друг от друга и закрываются в любой последовательности, лишь бы к моменту гейта все четыре были зелены на одном релизе.

## Traceability

Каждая задача несёт источники и T-классы в строке; сводно: фазовые гейты = `22` §4 дословно; TB-37 = запись 9 журнала; TB-20/35 = `ARC-M3`/§20.2; TB-33 = `AAR-M1`; TB-11 = `AAR-R2`; TB-04 (§12-роли) = `AAR-R1`; T05 (распределение по TB-05 «гонка версий», TB-16 «parallel callbacks», TB-19 «гонки applied», TB-29 «двойной timer claim») = `PLS-067` журнала стадии `26`. T01 (TB-07…TB-10 доменные команды и контракты эффектов; полный mock-PASS TB-13), T02 (I1/I5 — TB-08; I2 — TB-10; I3/I4 — TB-06; I6/I7 — TB-07; I8 — TB-06 и TB-07; I9 — TB-09; I10 — TB-11; regression model-proposal — TB-23; полный mock-PASS — TB-13), T20 (`BLOCKED_COVERAGE_OR_BUDGET` — TB-08; полный mock-PASS — TB-13) = `PLS-070`–`PLS-072` журнала стадии `26 v0.1`.

## Changelog v0 (проверка по контуру `15`)

| Находка | Severity | Закрытие |
|---|---|---|
| TBV-1 — errata PAV-1/PAV-2 не имели носителя-задачи и потерялись бы после приёмки | MINOR | TB-39 |
| TBV-2 — создание eval-fixtures подразумевалось внутри T10/T14/T15, но не было задачей с DoD, блокируя гейт фазы 3 неявно | MINOR | TB-24 отдельной L-задачей до TB-25 |
| TBV-3 — первичное заполнение Data Policy Registry датированными провайдерными условиями не было задачей, позволяя «пустой registry» дойти до первого вызова (fail-closed заблокировал бы всё, но без диагностики причины) | MINOR | TB-20 с явным DoD до TB-21 |

## Changelog v0.1 (по решению `PLS-067` журнала стадии `26`)

Единственное основание изменений — `PLS-067`. Зависимости, размеры и DoD задач, не перечисленных ниже, не изменены; тексты классов `22` §1 и гейтов `22` §4 не изменены.

| Изменение против v0 | Основание |
|---|---|
| Правила задач: добавлен абзац «Task-local DoD распределённого T-класса» — DoD задачи включает только её часть класса; полный `PASS` фиксируется отдельно записью журнала стадии | `PLS-067` |
| TB-05: `T01 частично, T05` → `T01 частично, T05 частично (гонка версий) — PLS-067` | `PLS-067` |
| TB-16: добавлено `T05 частично (parallel callbacks) — PLS-067` к `T09 полный` | `PLS-067` |
| TB-19: `T05 (гонки applied)` → `T05 частично (гонки applied) — PLS-067`; владение не изменилось, приведена формулировка | `PLS-067` |
| TB-29: добавлено `T05 частично (двойной timer claim) — PLS-067` к `T07` | `PLS-067` |
| Фаза 1: добавлено примечание о распределении T05 между **разными** фазами и о том, что размещение задачи в разделе фазы не отменяет её зависимостей и не позволяет обойти более ранний фазовый гейт | `PLS-067` |
| Фазы 2, 3, 5: добавлены отсылки к примечанию фазы 1 для TB-16, TB-19 и TB-29 соответственно | `PLS-067` |
| Критический путь: добавлен абзац о том, что гейт подключения реальных моделей требует завершения TB-16, TB-19 и TB-29 | `PLS-067` |
| Traceability: добавлено распределение T05 → `PLS-067` | `PLS-067` |

---

## Changelog v0.2 (по решениям `PLS-070`–`PLS-072` журнала стадии `26 v0.1`)

Единственное основание изменений — `PLS-070`, `PLS-071` и `PLS-072`. Зависимости, размеры и DoD задач, не перечисленных ниже, не изменены; тексты классов `22` §1 и гейтов `22` §4 не изменены; распределение T05 по `PLS-067` не изменено.

| Изменение против v0.1 | Основание |
|---|---|
| TB-06: `T01, T02, T20` → `T01 частично (правила и guards), T02 частично (I8 на границе command/state-machine), T20 частично (запрещённые маршруты §11 03)`; предмет задачи назван RuleCatalog из 78 правил и routing policy multi-target правил | `PLS-069`–`PLS-072` |
| TB-07: добавлены `T01 частично (доменные команды и контракты эффектов)` и `T02 частично (I8)`; источник — `17 v0.2` | `PLS-071`, `PLS-072` |
| TB-08: добавлен `T20 частично (BLOCKED_COVERAGE_OR_BUDGET)`; статус attempt назван явно | `PLS-070` |
| TB-09, TB-10: добавлено `T01 частично (контракты эффектов)` | `PLS-072` |
| TB-13: добавлены полный PASS T01 (full mock-contour PASS всех 78 правил), полный mock-PASS T02 и полный T20 на mock | `PLS-070`–`PLS-072` |
| TB-06: добавлены закрытый TriggerRegistry (65 триггеров), резолвер правила и guards, исполнительный API и commit-протоколы по outcome kind `17 v0.2` §§3.1–3.8; добавлена зависимость от TB-04a | `PLS-069` |
| Добавлена задача TB-04a — миграция схемы под `14 v0.3` (errata `DMV-4`–`DMV-6`): зависит от TB-04; от неё зависят TB-06 (creation protocol R01 и административный disposition) и TB-07 (idempotency-реестр `command_receipt`) | `14 v0.3` §17 |
| TB-05 сохраняет зависимость от TB-04 в том виде, в каком был принят: новая задача ретроактивно его зависимостей не меняет. Совместимость ledger-а с `14 v0.3` подтверждается DoD TB-04a — повторным прогоном тестов TB-05 на мигрированной схеме | `14 v0.3` §17 |
| TB-07: реестр команд 24 → 40 и покрытие 78/78 по `17 v0.2` §1.5 | `PLS-069` |
| TB-08, TB-09, TB-10, TB-11: закреплены части T02 (I1/I5, I9, I2, I10) — каждый инвариант I1–I10 имеет владельца | `PLS-071` |
| TB-23: добавлен отдельный regression-тест T02 на границе model-proposal с явной оговоркой, что полный T02 он не блокирует | `PLS-071` |
| Фаза 1: добавлено примечание о распределении T01, T02, T20, о неблокирующем статусе TB-26 и TB-29 и об исключении `PROVIDER_DATA_POLICY_BLOCKED` из T20 | `PLS-070`–`PLS-072` |
| Traceability: добавлены распределения T01, T02, T20 | `PLS-070`–`PLS-072` |


*Документ не изменяет спецификации; расхождения при реализации возвращаются как дефекты спецификаций через журнал стадии.*
